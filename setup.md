##Step 1 : Installation initialization
    
    (yarn install)

##Step 2 : Docker Engine:

    if present : 

    elif not-present:
        sudo pacman -S docker *Install docker engine*
        *Install docker compose*



##Step 3: To finally start the project
    docker compose down
    docker compose up -d
    yarn start
